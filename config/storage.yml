test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

# amazon:
#   service: S3
#   access_key_id: <%= ENV["S3_KEY"] %>
#   secret_access_key: <%= ENV["S3_SECRET"] %>
#   region: eu-west-1
#   bucket: path-of-economy-v1